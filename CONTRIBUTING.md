# Contributing

Keep sample assets original and portable. Run pytest and the packaged simulation before submitting changes. Put orchestration fixes in Light Show Manager and device protocol fixes in Govee LAN API Plus. Use local checks; do not add hosted Actions workloads by default.
