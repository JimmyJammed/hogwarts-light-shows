# Initial release

The previous repository contained only a README and license, so no application API migration exists. This release introduces version 1 show configuration and a simulation-first command line application. Keep user audio and private device mappings outside tracked sample assets.
